export async function closeMonth(groupId: string, month: string) {
  // aggregate scores
  // compute payouts
  // insert MonthlyOutcome
  return;
}

export async function getMonthlyResult(groupId: string, month: string) {
  return [];
}
