# Database connection string
# For Neon serverless PostgreSQL: postgresql://user:password@host/database?sslmode=require
DATABASE_URL="postgresql://user:password@localhost:5432/godcomplex?sslmode=require"

# Set to true to use PG adapter for serverless PostgreSQL (Neon, Supabase, etc.)
USE_PG_ADAPTER=true
